/* Root */
import Home from './views/Home';
// import About from './views/About';
// ijport Error404 from './views/Error404';

const main = [
	{
		path: '/',
		name: 'home',
		component: Home
	}
];

// const error = [
	// {
		// path: '*',
		// name: 'error',
		// component: Error404
	// }
// ];

export default [].concat(main);
